import { css } from 'lit'

export const primary = (alpha: number = 1) => css`rgba(65, 97, 101, ${alpha})`
export const secondary = (alpha: number = 1) => css`rgba(205, 83, 52, ${alpha})`

export const yellow = (alpha: number = 1) => css`rgba(242, 220, 93, ${alpha})`
export const green = (alpha: number = 1) => css`rgba(100, 182, 172, ${alpha})`
export const blue = (alpha: number = 1) => css`rgba(11, 57, 72, ${alpha})`
